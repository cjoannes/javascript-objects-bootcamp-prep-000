var playlist = { beyonce: "partition" }
function updatePlaylist(playlist, name, song) { playlist.adele = 'summer'; return Object.assign( {}, playlist, { [adele]: 'summer'}) }
function removeFromPlaylist(obj, name) { delete playlist.summer; return playlist }
